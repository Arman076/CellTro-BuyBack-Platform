===== D:\First-react-app\backend\prisma\schema-before-bom-fix.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema-before-bom-fix.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-bom-fix.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema-before-bom-fix.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema-before-bom-fix.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-bom-fix.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema-before-bom-fix.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema-before-bom-fix.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback     OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
===== D:\First-react-app\backend\prisma\schema-before-bom-fix.prisma : 856 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}

model CustomerVerifiedSession {
  id        String @id @default(cuid())
  tokenHash String @unique
  phone     String

  verifiedAt DateTime  @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id      Int    @id @default(autoincrement())
  pincode String @unique

  district String?
  state    String?

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  vendorMappings VendorServiceArea[]

  @@index([isActive])
  @@index([updatedAt])
===== D:\First-react-app\backend\prisma\schema-before-feedback-encoding-fix.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption? @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema-before-feedback-encoding-fix.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption? @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-feedback-encoding-fix.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema-before-feedback-encoding-fix.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema-before-feedback-encoding-fix.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-feedback-encoding-fix.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema-before-feedback-encoding-fix.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema-before-feedback-encoding-fix.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
===== D:\First-react-app\backend\prisma\schema-before-feedback-encoding-fix.prisma : 856 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}


model CustomerVerifiedSession {
  id        String   @id @default(cuid())
  tokenHash String   @unique
  phone     String

  verifiedAt DateTime @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id        Int      @id @default(autoincrement())
  pincode   String   @unique
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([isActive])
  @@index([updatedAt])
}

model CancellationReasonMaster {
  id               Int               @id @default(autoincrement())
  code             String
===== D:\First-react-app\backend\prisma\schema-before-routing-20260923-001800.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema-before-routing-20260923-001800.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-routing-20260923-001800.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema-before-routing-20260923-001800.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema-before-routing-20260923-001800.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-routing-20260923-001800.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema-before-routing-20260923-001800.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema-before-routing-20260923-001800.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback     OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
===== D:\First-react-app\backend\prisma\schema-before-routing-20260923-001800.prisma : 856 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}

model CustomerVerifiedSession {
  id        String @id @default(cuid())
  tokenHash String @unique
  phone     String

  verifiedAt DateTime  @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id      Int    @id @default(autoincrement())
  pincode String @unique

  district String?
  state    String?

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  vendorMappings VendorServiceArea[]

  @@index([isActive])
  @@index([updatedAt])
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-auth.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-auth.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-auth.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-auth.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-auth.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-auth.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-auth.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-auth.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback     OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-auth.prisma : 856 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}

model CustomerVerifiedSession {
  id        String @id @default(cuid())
  tokenHash String @unique
  phone     String

  verifiedAt DateTime  @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id      Int    @id @default(autoincrement())
  pincode String @unique

  district String?
  state    String?

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  vendorMappings VendorServiceArea[]

  @@index([isActive])
  @@index([updatedAt])
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-backup.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-backup.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-backup.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-backup.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-backup.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-backup.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-backup.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-backup.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback     OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
===== D:\First-react-app\backend\prisma\schema-before-vendor-approval-backup.prisma : 856 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}

model CustomerVerifiedSession {
  id        String @id @default(cuid())
  tokenHash String @unique
  phone     String

  verifiedAt DateTime  @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id      Int    @id @default(autoincrement())
  pincode String @unique

  district String?
  state    String?

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  vendorMappings VendorServiceArea[]

  @@index([isActive])
  @@index([updatedAt])
===== D:\First-react-app\backend\prisma\schema-before-vendor-auth-20260921-022524.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema-before-vendor-auth-20260921-022524.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-vendor-auth-20260921-022524.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema-before-vendor-auth-20260921-022524.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema-before-vendor-auth-20260921-022524.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-vendor-auth-20260921-022524.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema-before-vendor-auth-20260921-022524.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema-before-vendor-auth-20260921-022524.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback     OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
===== D:\First-react-app\backend\prisma\schema-before-vendor-auth-20260921-022524.prisma : 856 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}

model CustomerVerifiedSession {
  id        String @id @default(cuid())
  tokenHash String @unique
  phone     String

  verifiedAt DateTime  @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id      Int    @id @default(autoincrement())
  pincode String @unique

  district String?
  state    String?

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  vendorMappings VendorServiceArea[]

  @@index([isActive])
  @@index([updatedAt])
===== D:\First-react-app\backend\prisma\schema-before-vendor-otp-auth-backup.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema-before-vendor-otp-auth-backup.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-vendor-otp-auth-backup.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema-before-vendor-otp-auth-backup.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema-before-vendor-otp-auth-backup.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-vendor-otp-auth-backup.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema-before-vendor-otp-auth-backup.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema-before-vendor-otp-auth-backup.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback     OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
===== D:\First-react-app\backend\prisma\schema-before-vendor-otp-auth-backup.prisma : 856 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}

model CustomerVerifiedSession {
  id        String @id @default(cuid())
  tokenHash String @unique
  phone     String

  verifiedAt DateTime  @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id      Int    @id @default(autoincrement())
  pincode String @unique

  district String?
  state    String?

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  vendorMappings VendorServiceArea[]

  @@index([isActive])
  @@index([updatedAt])
===== D:\First-react-app\backend\prisma\schema-before-vendor.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption? @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema-before-vendor.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption? @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-vendor.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema-before-vendor.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema-before-vendor.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-before-vendor.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema-before-vendor.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema-before-vendor.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
===== D:\First-react-app\backend\prisma\schema-before-vendor.prisma : 856 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}


model CustomerVerifiedSession {
  id        String   @id @default(cuid())
  tokenHash String   @unique
  phone     String

  verifiedAt DateTime @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id        Int      @id @default(autoincrement())
  pincode   String   @unique

  district  String?
  state     String?

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([isActive])
  @@index([updatedAt])
}
===== D:\First-react-app\backend\prisma\schema-bom-backup.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption? @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema-bom-backup.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption? @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-bom-backup.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema-bom-backup.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema-bom-backup.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-bom-backup.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema-bom-backup.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema-bom-backup.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
===== D:\First-react-app\backend\prisma\schema-bom-backup.prisma : 856 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}


model CustomerVerifiedSession {
  id        String   @id @default(cuid())
  tokenHash String   @unique
  phone     String

  verifiedAt DateTime @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id        Int      @id @default(autoincrement())
  pincode   String   @unique

  district  String?
  state     String?

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([isActive])
  @@index([updatedAt])
}
===== D:\First-react-app\backend\prisma\schema-vendor-broken-backup.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption? @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema-vendor-broken-backup.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption? @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-vendor-broken-backup.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema-vendor-broken-backup.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema-vendor-broken-backup.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema-vendor-broken-backup.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema-vendor-broken-backup.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema-vendor-broken-backup.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
===== D:\First-react-app\backend\prisma\schema-vendor-broken-backup.prisma : 856 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}


model CustomerVerifiedSession {
  id        String   @id @default(cuid())
  tokenHash String   @unique
  phone     String

  verifiedAt DateTime @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id      Int    @id @default(autoincrement())
  pincode String @unique

  district String?
  state    String?

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  vendorMappings VendorServiceArea[]

  @@index([isActive])
===== D:\First-react-app\backend\prisma\schema.base-repaired.prisma : 227 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)
  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope        @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity    @default(NORMAL)
  agentRejectAllowed    Boolean                       @default(false)
  // Branching / sub-option configuration
  parentOptionId        Int?
  parentOption          QuestionnaireOption?          @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  childOptions          QuestionnaireOption[]         @relation("QuestionnaireOptionChildren")
  // Optional visual grouping for child issues (Physical Issues, Technical Issues, etc.)
  issueGroupId          Int?
  issueGroup            QuestionnaireIssueGroup?      @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups           QuestionnaireIssueGroup[]     @relation("QuestionnaireIssueGroupParent")

===== D:\First-react-app\backend\prisma\schema.base-repaired.prisma : 260 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)
  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope        @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity    @default(NORMAL)
  agentRejectAllowed    Boolean                       @default(false)
  // Branching / sub-option configuration
  parentOptionId        Int?
  parentOption          QuestionnaireOption?          @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  childOptions          QuestionnaireOption[]         @relation("QuestionnaireOptionChildren")
  // Optional visual grouping for child issues (Physical Issues, Technical Issues, etc.)
  issueGroupId          Int?
  issueGroup            QuestionnaireIssueGroup?      @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups           QuestionnaireIssueGroup[]     @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)
===== D:\First-react-app\backend\prisma\schema.base-repaired.prisma : 334 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema.base-repaired.prisma : 348 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema.base-repaired.prisma : 376 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.base-repaired.prisma : 451 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]
createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
  linkedinUrl  String?
===== D:\First-react-app\backend\prisma\schema.base-repaired.prisma : 660 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema.base-repaired.prisma : 732 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]
createdAt             DateTime                 @default(now())
  updatedAt             DateTime                 @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
  id            Int       @id @default(autoincrement())
  orderId       String
  order         SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)
  oldPickupDate DateTime  @db.Date
  newPickupDate DateTime  @db.Date
===== D:\First-react-app\backend\prisma\schema.before-identity-fix.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema.before-identity-fix.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.before-identity-fix.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema.before-identity-fix.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema.before-identity-fix.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.before-identity-fix.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema.before-identity-fix.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema.before-identity-fix.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
  id            Int       @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.before-identity-fix.prisma : 854 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}

model CustomerVerifiedSession {
  id        String @id @default(cuid())
  tokenHash String @unique
  phone     String

  verifiedAt DateTime  @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}
===== D:\First-react-app\backend\prisma\schema.before-identity-verified.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption? @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema.before-identity-verified.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption? @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.before-identity-verified.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema.before-identity-verified.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema.before-identity-verified.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.before-identity-verified.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema.before-identity-verified.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema.before-identity-verified.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?
  addressSnapshot       OrderAddressSnapshot?
  statusHistory         OrderStatusHistory[]
  reschedules           OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])
}

model OrderAddressSnapshot {
  id              Int                 @id @default(autoincrement())
  orderId         String              @unique
  order           SellOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  sourceAddressId Int?
  sourceAddress   CustomerAddress?    @relation(fields: [sourceAddressId], references: [id], onDelete: SetNull)
  fullName        String
  phone           String
  house           String
  street          String
  locality        String
  landmark        String?
  pincode         String
  city            String
  state           String
  type            CustomerAddressType
  createdAt       DateTime            @default(now())

  @@index([sourceAddressId])
  @@index([pincode])
}

model OrderStatusHistory {
  id        Int             @id @default(autoincrement())
  orderId   String
  order     SellOrder       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  status    SellOrderStatus
  note      String?
  createdAt DateTime        @default(now())

  @@index([orderId, createdAt])
  @@index([status])
}

model OrderRescheduleHistory {
  id            Int       @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.before-identity-verified.prisma : 854 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}


model CustomerVerifiedSession {
  id        String   @id @default(cuid())
  tokenHash String   @unique
  phone     String

  verifiedAt DateTime @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v3.prisma : 220 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v3.prisma : 253 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([issueCode])
}

model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v3.prisma : 285 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v3.prisma : 299 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v3.prisma : 327 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v3.prisma : 402 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v4.prisma : 225 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)
  // Branching / sub-option configuration
  parentOptionId   Int?
  parentOption     QuestionnaireOption?          @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  childOptions     QuestionnaireOption[]         @relation("QuestionnaireOptionChildren")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v4.prisma : 258 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)
  // Branching / sub-option configuration
  parentOptionId   Int?
  parentOption     QuestionnaireOption?          @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  childOptions     QuestionnaireOption[]         @relation("QuestionnaireOptionChildren")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([issueCode])
}

model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v4.prisma : 302 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v4.prisma : 316 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v4.prisma : 344 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v4.prisma : 419 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v5.prisma : 225 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)
  // Branching / sub-option configuration
  parentOptionId   Int?
  parentOption     QuestionnaireOption?          @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  childOptions     QuestionnaireOption[]         @relation("QuestionnaireOptionChildren")
  // Optional visual grouping for child issues (Physical Issues, Technical Issues, etc.)
  issueGroupId     Int?
  issueGroup       QuestionnaireIssueGroup?      @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups      QuestionnaireIssueGroup[]     @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v5.prisma : 258 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)
  // Branching / sub-option configuration
  parentOptionId   Int?
  parentOption     QuestionnaireOption?          @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  childOptions     QuestionnaireOption[]         @relation("QuestionnaireOptionChildren")
  // Optional visual grouping for child issues (Physical Issues, Technical Issues, etc.)
  issueGroupId     Int?
  issueGroup       QuestionnaireIssueGroup?      @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups      QuestionnaireIssueGroup[]     @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  parentOptionId Int
  parentOption   QuestionnaireOption @relation("QuestionnaireIssueGroupParent", fields: [parentOptionId], references: [id], onDelete: Cascade)

  childOptions QuestionnaireOption[] @relation("QuestionnaireIssueGroupChildren")

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v5.prisma : 325 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v5.prisma : 339 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v5.prisma : 367 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v5.prisma : 442 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v6.prisma : 230 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)
  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope        @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity    @default(NORMAL)
  agentRejectAllowed    Boolean                       @default(false)
  // Branching / sub-option configuration
  parentOptionId        Int?
  parentOption          QuestionnaireOption?          @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  childOptions          QuestionnaireOption[]         @relation("QuestionnaireOptionChildren")
  // Optional visual grouping for child issues (Physical Issues, Technical Issues, etc.)
  issueGroupId          Int?
  issueGroup            QuestionnaireIssueGroup?      @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups           QuestionnaireIssueGroup[]     @relation("QuestionnaireIssueGroupParent")

===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v6.prisma : 263 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType         QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue        Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger      QuestionnaireDeductionTrigger @default(SELECTED)
  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope        @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity    @default(NORMAL)
  agentRejectAllowed    Boolean                       @default(false)
  // Branching / sub-option configuration
  parentOptionId        Int?
  parentOption          QuestionnaireOption?          @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  childOptions          QuestionnaireOption[]         @relation("QuestionnaireOptionChildren")
  // Optional visual grouping for child issues (Physical Issues, Technical Issues, etc.)
  issueGroupId          Int?
  issueGroup            QuestionnaireIssueGroup?      @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups           QuestionnaireIssueGroup[]     @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v6.prisma : 337 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v6.prisma : 351 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v6.prisma : 379 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.before-questionnaire-v6.prisma : 454 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema.prisma : 234 =====
model QuestionnaireItem {
  id Int @id @default(autoincrement())

  code         String @unique
  name         String
  questionText String

  answerType QuestionnaireAnswerType

  displayOrder Int     @default(0)
  isRequired   Boolean @default(true)
  isActive     Boolean @default(true)

  applyToAllProducts Boolean @default(true)

  sectionId Int
  section   QuestionnaireSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  options QuestionnaireOption[]

  productMappings QuestionnaireItemProduct[]

  audiences QuestionnaireItemAudience[]

  conditions QuestionnaireCondition[] @relation("ConditionalItem")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
===== D:\First-react-app\backend\prisma\schema.prisma : 267 =====
model QuestionnaireOption {
  id Int @id @default(autoincrement())

  label     String
  value     String
  issueCode String?

  // Kept temporarily so current questionnaire code/data continues to work.
  deductionPercent Decimal @default(0) @db.Decimal(5, 2)

  deductionType    QuestionnaireDeductionType    @default(PERCENTAGE)
  deductionValue   Decimal                       @default(0) @db.Decimal(12, 2)
  deductionTrigger QuestionnaireDeductionTrigger @default(SELECTED)

  // V5 option visibility / pricing policy.
  // applicabilityTargetId is interpreted by applicabilityScope.
  applicabilityScope    QuestionnaireRuleScope     @default(GLOBAL)
  applicabilityTargetId Int?
  severity              QuestionnaireIssueSeverity @default(NORMAL)
  agentRejectAllowed    Boolean                    @default(false)

  // Branching / sub-option configuration
  parentOption   QuestionnaireOption?  @relation("QuestionnaireOptionChildren", fields: [parentOptionId], references: [id], onDelete: Cascade)
  parentOptionId Int?
  childOptions   QuestionnaireOption[] @relation("QuestionnaireOptionChildren")

  // Optional visual grouping for child issues
  issueGroupId Int?
  issueGroup   QuestionnaireIssueGroup?  @relation("QuestionnaireIssueGroupChildren", fields: [issueGroupId], references: [id], onDelete: SetNull)
  issueGroups  QuestionnaireIssueGroup[] @relation("QuestionnaireIssueGroupParent")

  showChildOptions      Boolean                         @default(false)
  childPrompt           String?
  requireChildSelection Boolean                         @default(false)
  minChildSelections    Int                             @default(0)
  maxChildSelections    Int?
  childSelectionMode    QuestionnaireChildSelectionMode @default(MULTI)

  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  dependentConditions QuestionnaireCondition[]

  deductionRules QuestionnaireDeductionRule[]
  capabilities   QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([itemId, value])
  @@index([parentOptionId])
  @@index([applicabilityScope, applicabilityTargetId])
  @@index([issueGroupId])
  @@index([issueCode])
}

model QuestionnaireIssueGroup {
  id           Int     @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.prisma : 344 =====
model QuestionnaireItemAudience {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  audienceId Int
  audience   QuestionnaireAudience @relation(fields: [audienceId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, audienceId])
}

model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
===== D:\First-react-app\backend\prisma\schema.prisma : 358 =====
model QuestionnaireItemProduct {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, productId])
}

model QuestionnaireCondition {
  id Int @id @default(autoincrement())

  itemId Int
  item   QuestionnaireItem @relation("ConditionalItem", fields: [itemId], references: [id], onDelete: Cascade)

  dependsOnOptionId Int
  dependsOnOption   QuestionnaireOption @relation(fields: [dependsOnOptionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([itemId, dependsOnOptionId])
}

model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
===== D:\First-react-app\backend\prisma\schema.prisma : 386 =====
model QuestionnaireDeductionRule {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  scope QuestionnaireRuleScope @default(PRODUCT)

  categoryId Int?
  category   Category? @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int?
  brand   Brand? @relation(fields: [brandId], references: [id], onDelete: Cascade)

  seriesId Int?
  series   ProductSeries? @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  productId Int?
  product   Product? @relation(fields: [productId], references: [id], onDelete: Cascade)

  variantId Int?
  variant   ProductVariant? @relation(fields: [variantId], references: [id], onDelete: Cascade)

  // Kept temporarily for backward compatibility.
  deductionPercent Decimal? @db.Decimal(5, 2)

  deductionType  QuestionnaireDeductionType @default(PERCENTAGE)
  deductionValue Decimal                    @default(0) @db.Decimal(12, 2)

  priority Int     @default(0)
  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([optionId])
  @@index([scope])
  @@index([categoryId])
  @@index([brandId])
  @@index([seriesId])
  @@index([productId])
  @@index([variantId])
  @@index([optionId, scope, isActive])
}

model DeviceCapability {
  id           Int     @id @default(autoincrement())
  code         String  @unique
  name         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  products ProductCapability[]
  options  QuestionnaireOptionCapability[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCapability {
  id Int @id @default(autoincrement())
===== D:\First-react-app\backend\prisma\schema.prisma : 461 =====
model QuestionnaireOptionCapability {
  id Int @id @default(autoincrement())

  optionId Int
  option   QuestionnaireOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  capabilityId Int
  capability   DeviceCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())

  @@unique([optionId, capabilityId])
  @@index([optionId])
  @@index([capabilityId])
}

model ProductSeries {
  id           Int     @id @default(autoincrement())
  name         String
  slug         String
  displayOrder Int     @default(0)
  isActive     Boolean @default(true)

  categoryId Int
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  brandId Int
  brand   Brand @relation(fields: [brandId], references: [id], onDelete: Cascade)

  products Product[]

  questionnaireDeductionRules QuestionnaireDeductionRule[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([categoryId, brandId, slug])
  @@index([categoryId])
  @@index([brandId])
}

model SiteSetting {
  id Int @id @default(autoincrement())

  companyName String  @default("CELLTRO")
  tagline     String? @default("Sell Smart. Sell Easy.")

  supportPhone   String?
  whatsappNumber String?
  supportEmail   String?
  businessEmail  String?

  officeAddress String?
  city          String?
  state         String?
  pincode       String?

  businessHours String?

  facebookUrl  String?
  instagramUrl String?
===== D:\First-react-app\backend\prisma\schema.prisma : 671 =====
enum SellOrderStatus {
  PICKUP_REQUESTED
  PICKUP_CONFIRMED
  PICKUP_STARTED
  INSPECTION_COMPLETED
  PAYMENT_COMPLETED
  COMPLETED
  CANCELLED
}

model Customer {
  id        Int               @id @default(autoincrement())
  phone     String            @unique
  addresses CustomerAddress[]
  orders    SellOrder[]
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt

  @@index([phone])
}

model CustomerAddress {
  id             Int                    @id @default(autoincrement())
  customerId     Int
  customer       Customer               @relation(fields: [customerId], references: [id], onDelete: Cascade)
  fullName       String
  house          String
  street         String
  locality       String
  landmark       String?
  pincode        String
  city           String
  state          String
  type           CustomerAddressType
  isActive       Boolean                @default(true)
  orderSnapshots OrderAddressSnapshot[]
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt

  @@index([customerId])
  @@index([pincode])
  @@index([isActive])
}

model PickupSlotTemplate {
  id           Int         @id @default(autoincrement())
  code         String      @unique
  label        String
  startTime    String
  endTime      String
  displayOrder Int         @default(0)
  isActive     Boolean     @default(true)
  orders       SellOrder[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([isActive, displayOrder])
}

model SellOrder {
  id                    String                   @id @default(cuid())
===== D:\First-react-app\backend\prisma\schema.prisma : 743 =====
  questionnaireSnapshot Json?
  status                SellOrderStatus          @default(PICKUP_REQUESTED)
  pickupDate            DateTime                 @db.Date
  pickupSlotId          Int
  pickupSlot            PickupSlotTemplate       @relation(fields: [pickupSlotId], references: [id], onDelete: Restrict)
  payoutMethod          PayoutMethod
  payoutUpiMobile       String?

  currentVendorId Int?
  currentVendor   Vendor? @relation(fields: [currentVendorId], references: [id], onDelete: Restrict)

  vendorAssignments OrderVendorAssignment[]

  addressSnapshot OrderAddressSnapshot?
  statusHistory   OrderStatusHistory[]
  reschedules     OrderRescheduleHistory[]

  enquiry      EnquirySession?
  cancellation OrderCancellation?
  feedback     OrderFeedback?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([customerId])
  @@index([orderNumber])
  @@index([status])
  @@index([pickupDate])
  @@index([pickupSlotId])
  @@index([createdAt])

  @@index([currentVendorId, status, createdAt])
}


enum OrderVendorAssignmentSource {
  AUTO
  MANUAL
  REROUTE
}

enum OrderVendorAssignmentReason {
  PINCODE_PRIORITY
  MANUAL_ADMIN
  REROUTE
}

enum OrderVendorUnassignmentReason {
  MANUAL
  VENDOR_INACTIVE
  VENDOR_SUSPENDED
  SERVICE_AREA_REMOVED
  REROUTED
}

model OrderVendorAssignment {
  id Int @id @default(autoincrement())

  orderId String
  order   SellOrder @relation(fields: [orderId], references: [id], onDelete: Restrict)

===== D:\First-react-app\backend\prisma\schema.prisma : 910 =====
  questionnaireSnapshot Json?

  @@index([phone])
  @@index([questionnaireCompletedAt])
  @@index([otpVerifiedAt])
  @@index([quoteViewedAt, orderId])
  @@index([createdAt])
  @@index([verifiedSessionId, quoteViewedAt])
  @@index([identityVerifiedAt])
}

model OrderCancellation {
  id      Int    @id @default(autoincrement())
  orderId String @unique

  order SellOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  actor CancellationActor

  reasonCode String?
  reasonText String?

  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([actor, createdAt])
}

model CustomerVerifiedSession {
  id        String @id @default(cuid())
  tokenHash String @unique
  phone     String

  verifiedAt DateTime  @default(now())
  expiresAt  DateTime
  revokedAt  DateTime?

  enquiries EnquirySession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([phone, expiresAt])
  @@index([expiresAt, revokedAt])
}

model ServiceablePincode {
  id      Int    @id @default(autoincrement())
  pincode String @unique

  district String?
  state    String?

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  vendorMappings VendorServiceArea[]

  @@index([isActive])
  @@index([updatedAt])
===== D:\First-react-app\backend\src\orders\orders.service.ts : 1445 =====
                questionnaireSnapshot:
                  (
                    enquiry.questionnaireSnapshot ??
                    undefined
                  ) as any,

                pickupDate,

                pickupSlotId:
                  slot.id,

                payoutMethod:
                  payoutMethod as
                    | "CASH"
                    | "UPI",

                payoutUpiMobile,

                addressSnapshot: {
                  create: {
                    sourceAddressId:
                      address.id,

                    fullName:
                      address.fullName,

                    phone:
                      verified.phone,

                    house:
                      address.house,

                    street:
                      address.street,

                    locality:
                      address.locality,

                    landmark:
                      address.landmark,

                    pincode:
                      address.pincode,

                    city:
                      address.city,

                    state:
                      address.state,

                    type:
                      address.type,
                  },
                },

                statusHistory: {
                  create: {
                    status:
                      "PICKUP_REQUESTED",

                    note:
===== D:\First-react-app\backend\src\orders\orders.service.ts : 1447 =====
                    enquiry.questionnaireSnapshot ??
                    undefined
                  ) as any,

                pickupDate,

                pickupSlotId:
                  slot.id,

                payoutMethod:
                  payoutMethod as
                    | "CASH"
                    | "UPI",

                payoutUpiMobile,

                addressSnapshot: {
                  create: {
                    sourceAddressId:
                      address.id,

                    fullName:
                      address.fullName,

                    phone:
                      verified.phone,

                    house:
                      address.house,

                    street:
                      address.street,

                    locality:
                      address.locality,

                    landmark:
                      address.landmark,

                    pincode:
                      address.pincode,

                    city:
                      address.city,

                    state:
                      address.state,

                    type:
                      address.type,
                  },
                },

                statusHistory: {
                  create: {
                    status:
                      "PICKUP_REQUESTED",

                    note:
                      "Pickup request created by customer.",
                  },
===== D:\First-react-app\backend\src\questionnaire\questionnaire-quote.controller.ts : 451 =====
                  questionnaireSnapshot:
                    body?.answers ??
                    undefined,

                  questionnaireCompletedAt:
                    existingJourney
                      .questionnaireCompletedAt ??
                    now,

                  /*
                   * 24-hour verified session proves identity,
                   * but this is NOT a fresh SMS OTP verification.
                   */
                  identityVerifiedAt:
                    existingJourney
                      .identityVerifiedAt ??
                    now,

                  /*
                   * Preserve the first quote-view timestamp.
                   * Requoting / changing questionnaire answers
                   * must not restart the 10-minute inquiry clock.
                   */
                  quoteViewedAt:
                    existingJourney
                      .quoteViewedAt ??
                    now,

                  quoteAmount:
                    Number(
                      quote.finalPrice,
                    ),
                },
              })
            : await this.prisma.enquirySession.create({
                data: {
                  sessionId:
                    randomUUID(),

                  phone,
                  productId,
                  variantId,

                  verifiedSessionId:
                    verifiedSession.id,

                  questionnaireSnapshot:
                    body?.answers ??
                    undefined,

                  questionnaireCompletedAt:
                    now,

                  /*
                   * Identity trusted through existing 24h session.
                   * otpVerifiedAt intentionally stays NULL because
                   * no SMS OTP was verified for this device journey.
                   */
                  identityVerifiedAt:
                    now,

===== D:\First-react-app\backend\src\questionnaire\questionnaire-quote.controller.ts : 497 =====
                  questionnaireSnapshot:
                    body?.answers ??
                    undefined,

                  questionnaireCompletedAt:
                    now,

                  /*
                   * Identity trusted through existing 24h session.
                   * otpVerifiedAt intentionally stays NULL because
                   * no SMS OTP was verified for this device journey.
                   */
                  identityVerifiedAt:
                    now,

                  quoteViewedAt:
                    now,

                  quoteAmount:
                    Number(
                      quote.finalPrice,
                    ),
                },
              });

        return {
          existingOrder:
            false,

          otpRequired:
            false,

          sessionId:
            journey.sessionId,

          customerSessionExpiresAt:
            verifiedSession.expiresAt.toISOString(),

          ...quote,
        };
      }
    }

    const result =
      await this.service.sendOtp(
        body,
      );

    const sessionId =
      String(
        result?.sessionId ??
          "",
      ).trim();

    if (!sessionId) {
      throw new BadRequestException(
        "OTP session could not be created.",
      );
    }

    const now =
===== D:\First-react-app\backend\src\questionnaire\questionnaire-quote.controller.ts : 580 =====
          questionnaireSnapshot:
            body?.answers ??
            undefined,

          questionnaireCompletedAt:
            existingJourney
              .questionnaireCompletedAt ??
            now,
        },
      });
    } else {
      await this.prisma.enquirySession.create({
        data: {
          sessionId,
          phone,
          productId,
          variantId,

          questionnaireSnapshot:
            body?.answers ??
            undefined,

          questionnaireCompletedAt:
            now,

          otpSentAt:
            now,
        },
      });
    }

    return {
      ...result,

      existingOrder:
        false,

      otpRequired:
        true,
    };
  }

  @Post("verify-otp")
  async verifyOtp(
    @Body()
    body: {
      sessionId: string;
      otp: string;
      customerSessionToken?: string;
    },

    @Req()
    request: Request,

    @Res({
      passthrough: true,
    })
    response: Response,
  ) {
    const result =
      await this.service.verifyOtp(
===== D:\First-react-app\backend\src\questionnaire\questionnaire-quote.controller.ts : 598 =====
          questionnaireSnapshot:
            body?.answers ??
            undefined,

          questionnaireCompletedAt:
            now,

          otpSentAt:
            now,
        },
      });
    }

    return {
      ...result,

      existingOrder:
        false,

      otpRequired:
        true,
    };
  }

  @Post("verify-otp")
  async verifyOtp(
    @Body()
    body: {
      sessionId: string;
      otp: string;
      customerSessionToken?: string;
    },

    @Req()
    request: Request,

    @Res({
      passthrough: true,
    })
    response: Response,
  ) {
    const result =
      await this.service.verifyOtp(
        body.sessionId,
        body.otp,
      );

    const phone =
      this.normalizePhone(
        result?.phone,
      );

    if (
      !/^[6-9]\d{9}$/.test(
        phone,
      )
    ) {
      throw new BadRequestException(
        "Verified mobile number is invalid.",
      );
    }
===== D:\First-react-app\backend\src\questionnaire\questionnaire-quote.service.ts : 61 =====
export class QuestionnaireQuoteService {
  private readonly sessions = new Map<string, OtpSession>();
  constructor(private readonly prisma: PrismaService) {}

  private toNumber(value: unknown) {
    const n = Number(value);
    return Number.isFinite(n) ? n : 0;
  }

  private hash(value: string) {
    return createHash('sha256').update(value).digest('hex');
  }

  private cleanPhone(value: string) {
    return String(value || '').replace(/\D/g, '');
  }

  private amount(type: 'PERCENTAGE' | 'FIXED', value: number, basePrice: number) {
    return type === 'FIXED'
      ? Math.max(0, value)
      : Math.max(0, (basePrice * value) / 100);
  }

  private optionApplicable(
    option: any,
    context: {
      categoryId: number;
      brandId: number;
      seriesId: number | null;
      productId: number;
      variantId: number;
    },
  ) {
    const scope = option.applicabilityScope || 'GLOBAL';
    const target = option.applicabilityTargetId == null
      ? null
      : Number(option.applicabilityTargetId);

    if (scope === 'GLOBAL') return true;
    if (target === null) return false;
    if (scope === 'CATEGORY') return target === context.categoryId;
    if (scope === 'BRAND') return target === context.brandId;
    if (scope === 'SERIES') return context.seriesId !== null && target === context.seriesId;
    if (scope === 'PRODUCT') return target === context.productId;
    if (scope === 'VARIANT') return target === context.variantId;
    return false;
  }

  private capabilityAllowed(option: any, capabilityIds: Set<number>) {
    const required = (option.capabilities || []).map((x: any) => x.capabilityId);
    return !required.length || required.some((id: number) => capabilityIds.has(id));
  }

  private resolveRule(option: any, context: any) {
    const rules = (option.deductionRules || [])
      .filter((rule: any) => rule.isActive)
      .sort((a: any, b: any) => (b.priority - a.priority) || (b.id - a.id));

    const scopes: Array<[string, (r: any) => boolean]> = [
      ['VARIANT', (r) => r.variantId === context.variantId],
      ['PRODUCT', (r) => r.productId === context.productId],
===== D:\First-react-app\backend\src\super-admin\super-admin-enquiries.service.ts : 505 =====
          questionnaireSnapshot:
            true,

          createdAt: true,
          updatedAt: true,

          product: {
            select: {
              id: true,
              name: true,
            },
          },

          variant: {
            select: {
              id: true,

              values: {
                select: {
                  attribute: {
                    select: {
                      name: true,
                    },
                  },

                  option: {
                    select: {
                      value: true,
                    },
                  },
                },
              },
            },
          },
        },
      });

    if (!enquiry) {
      throw new NotFoundException(
        "Active enquiry not found.",
      );
    }

    return {
      id: enquiry.id,

      sessionId:
        enquiry.sessionId,

      phone:
        enquiry.phone,

      productId:
        enquiry.productId,

      productName:
        enquiry.product?.name ??
        null,

      variantId:
        enquiry.variantId,
===== D:\First-react-app\backend\src\super-admin\super-admin-enquiries.service.ts : 601 =====
      questionnaireSnapshot:
        enquiry.questionnaireSnapshot,

      createdAt:
        enquiry.createdAt,

      updatedAt:
        enquiry.updatedAt,

      status:
        "ACTIVE_ENQUIRY",

      abandonmentMinutes:
        ENQUIRY_DELAY_MINUTES,
    };
  }
}
===== D:\First-react-app\backend\src\super-admin\super-admin-enquiries.service.ts : 602 =====
        enquiry.questionnaireSnapshot,

      createdAt:
        enquiry.createdAt,

      updatedAt:
        enquiry.updatedAt,

      status:
        "ACTIVE_ENQUIRY",

      abandonmentMinutes:
        ENQUIRY_DELAY_MINUTES,
    };
  }
}
===== D:\First-react-app\backend\src\super-admin\super-admin-orders.service.ts : 21 =====
type QuestionnaireSnapshotAnswer = {
  itemId?: unknown;
  optionId?: unknown;
  optionIds?: unknown;
  childOptionIds?: unknown;
};

const VALID_STATUSES = new Set([
  "PICKUP_REQUESTED",
  "PICKUP_CONFIRMED",
  "PICKUP_STARTED",
  "INSPECTION_COMPLETED",
  "PAYMENT_COMPLETED",
  "COMPLETED",
  "CANCELLED",
]);


const PENDING_STATUSES = [
  "PICKUP_REQUESTED",
  "PICKUP_CONFIRMED",
  "PICKUP_STARTED",
  "INSPECTION_COMPLETED",
  "PAYMENT_COMPLETED",
] as const;

const VALID_STATUS_GROUPS =
  new Set([
    "PENDING",
    "COMPLETED",
    "CANCELLED",
  ]);

@Injectable()
export class SuperAdminOrdersService {
  constructor(
    private readonly prisma:
      PrismaService,
  ) {}

  private toNumber(
    value: unknown,
  ): number {
    const number = Number(value);

    return Number.isFinite(number)
      ? number
      : 0;
  }

  private positiveInteger(
    value: unknown,
    fallback: number,
    maximum: number,
  ) {
    const parsed =
      Number.parseInt(
        String(value ?? ""),
        10,
      );

===== D:\First-react-app\backend\src\super-admin\super-admin-orders.service.ts : 142 =====
  ): QuestionnaireSnapshotAnswer[] {
    if (!Array.isArray(value)) {
      return [];
    }

    return value.filter(
      (
        entry,
      ): entry is QuestionnaireSnapshotAnswer =>
        Boolean(
          entry &&
            typeof entry ===
              "object",
        ),
    );
  }

  private numberArray(
    value: unknown,
  ): number[] {
    if (!Array.isArray(value)) {
      return [];
    }

    return [
      ...new Set(
        value
          .map((item) =>
            Number(item),
          )
          .filter((item) =>
            Number.isInteger(item),
          ),
      ),
    ];
  }

  private snapshotItemId(
    value: unknown,
  ) {
    const id =
      Number(value);

    return Number.isInteger(id)
      ? id
      : null;
  }

  private snapshotSelectedIds(
    answer:
      QuestionnaireSnapshotAnswer,
  ) {
    const ids: number[] = [];

    const optionId =
      Number(answer.optionId);

    if (
      Number.isInteger(
        optionId,
      )
===== D:\First-react-app\backend\src\super-admin\super-admin-orders.service.ts : 150 =====
      ): entry is QuestionnaireSnapshotAnswer =>
        Boolean(
          entry &&
            typeof entry ===
              "object",
        ),
    );
  }

  private numberArray(
    value: unknown,
  ): number[] {
    if (!Array.isArray(value)) {
      return [];
    }

    return [
      ...new Set(
        value
          .map((item) =>
            Number(item),
          )
          .filter((item) =>
            Number.isInteger(item),
          ),
      ),
    ];
  }

  private snapshotItemId(
    value: unknown,
  ) {
    const id =
      Number(value);

    return Number.isInteger(id)
      ? id
      : null;
  }

  private snapshotSelectedIds(
    answer:
      QuestionnaireSnapshotAnswer,
  ) {
    const ids: number[] = [];

    const optionId =
      Number(answer.optionId);

    if (
      Number.isInteger(
        optionId,
      )
    ) {
      ids.push(optionId);
    }

    ids.push(
      ...this.numberArray(
        answer.optionIds,
      ),
===== D:\First-react-app\backend\src\super-admin\super-admin-orders.service.ts : 192 =====
      QuestionnaireSnapshotAnswer,
  ) {
    const ids: number[] = [];

    const optionId =
      Number(answer.optionId);

    if (
      Number.isInteger(
        optionId,
      )
    ) {
      ids.push(optionId);
    }

    ids.push(
      ...this.numberArray(
        answer.optionIds,
      ),
    );

    return [...new Set(ids)];
  }

  private snapshotChildIds(
    answer:
      QuestionnaireSnapshotAnswer,
  ) {
    return this.numberArray(
      answer.childOptionIds,
    );
  }

  async getOrders(
    input: GetOrdersInput = {},
  ) {
    const page =
      this.positiveInteger(
        input.page,
        1,
        1_000_000,
      );

    const limit =
      this.positiveInteger(
        input.limit,
        20,
        100,
      );

    const search =
      String(
        input.search ?? "",
      )
        .trim()
        .slice(0, 100);

    const status =
      String(
        input.status ?? "",
      )
===== D:\First-react-app\backend\src\super-admin\super-admin-orders.service.ts : 218 =====
      QuestionnaireSnapshotAnswer,
  ) {
    return this.numberArray(
      answer.childOptionIds,
    );
  }

  async getOrders(
    input: GetOrdersInput = {},
  ) {
    const page =
      this.positiveInteger(
        input.page,
        1,
        1_000_000,
      );

    const limit =
      this.positiveInteger(
        input.limit,
        20,
        100,
      );

    const search =
      String(
        input.search ?? "",
      )
        .trim()
        .slice(0, 100);

    const status =
      String(
        input.status ?? "",
      )
        .trim()
        .toUpperCase();

    const from =
      String(
        input.from ?? "",
      ).trim();

    const to =
      String(
        input.to ?? "",
      ).trim();

    if (
      status &&
      !VALID_STATUSES.has(status)
    ) {
      throw new BadRequestException(
        "Invalid order status.",
      );
    }

    let fromDate:
      | Date
      | undefined;

===== D:\First-react-app\backend\src\super-admin\super-admin-orders.service.ts : 699 =====
             * - questionnaireSnapshot
             * - totalDeduction
             *
             * It does NOT store immutable
             * per-answer applied deduction.
             *
             * Current pricing rules may have
             * changed after this order.
             *
             * Therefore we deliberately do not
             * fabricate a historical per-question
             * deduction here.
             */
            deductionAmount:
              null,

            deductionBreakdownAvailable:
              false,

            _sort: {
              section:
                item?.section
                  ?.displayOrder ??
                999999,

              question:
                item?.displayOrder ??
                999999,

              snapshotIndex,
            },
          };
        },
      )
      .filter(Boolean)
      .sort(
        (
          left: any,
          right: any,
        ) =>
          left._sort.section -
            right._sort.section ||
          left._sort.question -
            right._sort.question ||
          left._sort
            .snapshotIndex -
            right._sort
              .snapshotIndex,
      )
      .map(
        ({
          _sort,
          ...answer
        }: any) =>
          answer,
      );
  }

  async getOrderDetails(
    orderNumber: string,
  ) {
===== D:\First-react-app\backend\src\super-admin\super-admin-orders.service.ts : 820 =====
        order.questionnaireSnapshot,
      );

    return {
      id: order.id,

      orderNumber:
        order.orderNumber,

      customerId:
        order.customerId,

      productId:
        order.productId,

      variantId:
        order.variantId,

      productName:
        order.productName,

      productImage:
        order.productImage,

      variantLabel:
        order.variantLabel,

      basePrice:
        this.toNumber(
          order.basePrice,
        ),

      totalDeduction:
        this.toNumber(
          order.totalDeduction,
        ),

      finalPrice:
        this.toNumber(
          order.finalPrice,
        ),

      status:
        order.status,

      pickupDate:
        order.pickupDate,

      pickupSlotId:
        order.pickupSlotId,

      payoutMethod:
        order.payoutMethod,

      payoutUpiMobile:
        order.payoutUpiMobile,

      createdAt:
        order.createdAt,

      updatedAt:
===== D:\First-react-app\backend\src\vendor-orders\vendor-orders.service.ts : 586 =====
          questionnaireSnapshot:
            true,

          status: true,

          pickupDate: true,

          payoutMethod: true,
          payoutUpiMobile: true,

          createdAt: true,
          updatedAt: true,

          pickupSlot: {
            select: {
              code: true,
              label: true,
              startTime: true,
              endTime: true,
            },
          },

          addressSnapshot: {
            select: {
              fullName: true,
              phone: true,

              house: true,
              street: true,
              locality: true,
              landmark: true,

              pincode: true,
              city: true,
              state: true,
              type: true,
            },
          },

          statusHistory: {
            orderBy: {
              createdAt: "asc",
            },

            select: {
              id: true,
              status: true,
              note: true,
              createdAt: true,
            },
          },

          reschedules: {
            orderBy: {
              createdAt: "desc",
            },

            select: {
              id: true,

              oldPickupDate: true,
===== D:\First-react-app\backend\src\vendor-orders\vendor-orders.service.ts : 725 =====
        order.questionnaireSnapshot,

      status:
        order.status,

      pickup: {
        date:
          order.pickupDate,

        slot:
          order.pickupSlot,
      },

      payout: {
        method:
          order.payoutMethod,

        upiMobile:
          order.payoutUpiMobile,
      },

      customer:
        order.addressSnapshot
          ? {
              name:
                order.addressSnapshot
                  .fullName,

              phone:
                order.addressSnapshot
                  .phone,
            }
          : null,

      address:
        order.addressSnapshot,

      statusHistory:
        order.statusHistory,

      reschedules:
        order.reschedules,

      assignmentHistory:
        order.vendorAssignments,

      /*
       * Agent module is intentionally
       * not fabricated here.
       *
       * Later this response will contain
       * agentAssignment + inspections +
       * requotes from real DB records.
       */
      agent: null,

      createdAt:
        order.createdAt,

      updatedAt:
        order.updatedAt,
